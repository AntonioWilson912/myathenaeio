<Window
    x:Class="MyAthenaeio.Views.Genres.GenreDetailWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:MyAthenaeio.Views.Genres"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="Genre Details"
    Width="600"
    Height="500"
    WindowStartupLocation="CenterOwner"
    mc:Ignorable="d">
    <Window.Resources>
        <ResourceDictionary Source="/Resources/Styles.xaml" />
    </Window.Resources>
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Style="{StaticResource RusticBorderStyle}">
            <Grid>
                <TextBlock
                    VerticalAlignment="Center"
                    FontSize="20"
                    FontWeight="Bold"
                    Foreground="White"
                    Text="Genre Information" />
                <Button
                    HorizontalAlignment="Right"
                    Click="Edit_Click"
                    Content="Edit Genre" />
            </Grid>
        </Border>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <StackPanel VerticalAlignment="Center" Orientation="Horizontal">
                    <Label VerticalAlignment="Center" Content="Genre Name:" />
                    <TextBlock
                        x:Name="NameTextBlock"
                        VerticalAlignment="Center"
                        FontWeight="Bold" />
                </StackPanel>

                <GroupBox Margin="5" Header="Books in Library">
                    <ListBox
                        x:Name="BooksListBox"
                        Height="200"
                        MouseDoubleClick="BooksListBox_MouseDoubleClick">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel>
                                    <TextBlock FontWeight="Bold" Text="{Binding Title}" />
                                    <TextBlock
                                        FontSize="11"
                                        Foreground="Gray"
                                        Text="{Binding PublicationYear, StringFormat='Published: {0}'}" />
                                </StackPanel>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </GroupBox>
            </StackPanel>
        </ScrollViewer>

        <Button
            Grid.Row="2"
            Width="100"
            Margin="0,15,0,0"
            HorizontalAlignment="Right"
            Click="Close_Click"
            Content="Close" />
    </Grid>
</Window>
