<Window
    x:Class="MyAthenaeio.Views.Borrowers.BorrowerAddDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:MyAthenaeio.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="Add Borrower"
    Width="500"
    Height="400"
    WindowStartupLocation="CenterOwner"
    mc:Ignorable="d">
    <Window.Resources>
        <ResourceDictionary Source="/Resources/Styles.xaml" />
    </Window.Resources>
    <Grid Background="{StaticResource DarkAcademiaBackground}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Style="{StaticResource DarkAcademiaBorderStyle}">
            <TextBlock Style="{StaticResource HeaderTextBlockStyle}" Text="Add New Borrower" />
        </Border>

        <Grid Grid.Row="1" Margin="15">
            <Grid.Resources>
                <Style BasedOn="{StaticResource {x:Type Label}}" TargetType="Label">
                    <Setter Property="Margin" Value="0,0,10,10" />
                </Style>
                <Style BasedOn="{StaticResource {x:Type TextBox}}" TargetType="TextBox">
                    <Setter Property="Margin" Value="0,0,0,10" />
                </Style>
            </Grid.Resources>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Label
                Grid.Row="0"
                Grid.Column="0"
                Content="Name:*" />
            <TextBox
                x:Name="NameTextBox"
                Grid.Row="0"
                Grid.Column="1" />

            <Label
                Grid.Row="1"
                Grid.Column="0"
                Content="Email:" />
            <TextBox
                x:Name="EmailTextBox"
                Grid.Row="1"
                Grid.Column="1" />

            <Label
                Grid.Row="2"
                Grid.Column="0"
                Content="Phone:" />
            <TextBox
                x:Name="PhoneTextBox"
                Grid.Row="2"
                Grid.Column="1"
                LostFocus="PhoneTextBox_LostFocus" />

            <Label
                Grid.Row="3"
                Grid.Column="0"
                Content="Notes:" />
            <TextBox
                x:Name="NotesTextBox"
                Grid.Row="3"
                Grid.Column="1"
                Height="60"
                AcceptsReturn="True"
                TextWrapping="Wrap"
                VerticalScrollBarVisibility="Auto" />
        </Grid>

        <StackPanel
            Grid.Row="2"
            Margin="15"
            HorizontalAlignment="Right"
            Orientation="Horizontal">
            <Button Click="Add_Click" Content="Add" />
            <Button Click="Cancel_Click" Content="Cancel" />
        </StackPanel>
    </Grid>
</Window>