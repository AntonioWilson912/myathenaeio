<Window
    x:Class="MyAthenaeio.Views.Loans.CheckoutBookDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:MyAthenaeio.Views.Loans"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="Check Out a Book"
    Width="600"
    Height="450"
    WindowStartupLocation="CenterOwner"
    mc:Ignorable="d">
    <Window.Resources>
        <ResourceDictionary Source="/Resources/Styles.xaml" />
    </Window.Resources>

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!--  Header  -->
        <Border
            Grid.Row="0"
            Margin="0,0,0,15"
            Padding="15"
            Background="{StaticResource RusticAccent}"
            CornerRadius="5">
            <TextBlock
                FontSize="18"
                FontWeight="Bold"
                Foreground="White"
                Text="Check Out a Book" />
        </Border>

        <!--  Book Info  -->
        <Border
            Grid.Row="1"
            Margin="0,0,0,10"
            Padding="10"
            Background="#F5F5F5"
            BorderBrush="#E0E0E0"
            BorderThickness="1"
            CornerRadius="3">
            <StackPanel>
                <TextBlock
                    x:Name="BookTitleTextBlock"
                    FontSize="16"
                    FontWeight="SemiBold"
                    TextWrapping="Wrap" />
                <TextBlock
                    x:Name="CopyInfoTextBlock"
                    Margin="0,5,0,0"
                    FontSize="12"
                    Foreground="Gray" />
            </StackPanel>
        </Border>

        <!--  Checkout Form  -->
        <GroupBox
            Grid.Row="2"
            Padding="10"
            Header="Checkout Information">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="120" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <Label
                    Grid.Row="0"
                    Grid.Column="0"
                    Content="Borrower:*" />
                <ComboBox
                    x:Name="BorrowerComboBox"
                    Grid.Row="0"
                    Grid.Column="1"
                    Margin="5"
                    IsEditable="True"
                    IsTextSearchEnabled="True"
                    SelectionChanged="BorrowerComboBox_SelectionChanged"
                    TextSearch.TextPath="Name">
                    <ComboBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{Binding Name}" />
                                <TextBlock
                                    FontWeight="Bold"
                                    Foreground="Red"
                                    Text=" (Overdue)"
                                    Visibility="{Binding HasOverdueLoans, Converter={StaticResource BooleanToVisibilityConverter}}" />
                            </StackPanel>
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>

                <Label
                    Grid.Row="1"
                    Grid.Column="0"
                    Content="Email:" />
                <TextBlock
                    x:Name="BorrowerEmailTextBlock"
                    Grid.Row="1"
                    Grid.Column="1"
                    Margin="5"
                    VerticalAlignment="Center"
                    Foreground="Gray" />

                <Label
                    Grid.Row="2"
                    Grid.Column="0"
                    Content="Checkout Date:*" />
                <DatePicker
                    x:Name="CheckoutDatePicker"
                    Grid.Row="2"
                    Grid.Column="1"
                    Margin="5" />

                <Label
                    Grid.Row="3"
                    Grid.Column="0"
                    Content="Due Date:*" />
                <DatePicker
                    x:Name="DueDatePicker"
                    Grid.Row="3"
                    Grid.Column="1"
                    Margin="5" />

                <Label
                    Grid.Row="4"
                    Grid.Column="0"
                    Content="Notes:" />
                <TextBox
                    x:Name="NotesTextBox"
                    Grid.Row="4"
                    Grid.Column="1"
                    Height="80"
                    Margin="5"
                    AcceptsReturn="True"
                    TextWrapping="Wrap"
                    VerticalScrollBarVisibility="Auto" />
            </Grid>
        </GroupBox>

        <!--  Action Buttons  -->
        <StackPanel
            Grid.Row="3"
            Margin="0,15,0,0"
            HorizontalAlignment="Right"
            Orientation="Horizontal">
            <Button
                Width="120"
                Click="Checkout_Click"
                Content="Check Out" />
            <Button
                Width="100"
                Click="Cancel_Click"
                Content="Cancel" />
        </StackPanel>
    </Grid>
</Window>